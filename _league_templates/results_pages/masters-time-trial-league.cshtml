@using static LeagueTables.Helpers;
@{
	var StartDate = Model.StartDate.ToString("%d") + DateSuffix(Model.StartDate) + Model.StartDate.ToString(@" MMMM, yyyy");
	var EndDate = Model.EndDate.ToString("%d") + DateSuffix(Model.EndDate) + Model.EndDate.ToString(@" MMMM, yyyy");
	var DateData = Model.StartDate.ToString("yyyy-MM-dd-HH-mm");
	var EventDateTime = Model.StartDate.ToString(@"dddd, h:mmtt UTC");
}
---
layout: default
date: @DateTime.UtcNow.ToString(@"dd/MM/yyyy")
---

<h2 id='results'>Masters Time Trial Racing League: @(Model.Year) Series @(Model.Number)</h2>

<p>
	These are the handicapped results for Masters, based on VTTA times. When viewing on ZwiftPower, sort by
	the VHT/VTTA column, which is used for generating these results.
</p>

<p>
	You can also view the standard category based results available <a href='/results/current/time-trial-league'>for all riders</a>.
</p>

<p>Covers races from <strong>@StartDate</strong> &mdash; <strong>@EndDate</strong>.</p>
<p>Last updated: {{ page.date | date: "%A, %B %e" }}</p>

{% include standings.html riders=site.data.@(Model.Year).series_@(Model.Number).masterstimetrial teams=site.data.@(Model.Year).series_@(Model.Number).masterstimetrialteam %}

{% include datetime.html %}
